$(document).ready(function() {
	
	$('#start_date').datepicker({
		 todayBtn: true,
		 autoclose: true,
		 todayHighlight: true,
		 format: 'MM d, yyyy'
	});
	
	$('#end_date').datepicker({
		 todayBtn: true,
		 autoclose: true,
		 todayHighlight: true,
		 format: 'MM d, yyyy'
	});
	
		
})

